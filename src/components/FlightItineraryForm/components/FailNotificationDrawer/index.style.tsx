import { Box, styled } from '@mui/material';

export const ContainerStyles = styled(Box)({
  padding: '20px',
  display: 'flex',
  flexDirection: 'column',
  gap: '24px',
});

export const ButtonGroupContainerStyles = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  gap: '4px',
});
